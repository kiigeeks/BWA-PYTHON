# Variabel terpisah ini dibutuhkan oleh logic.py
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=

# PENGATURAN KONEKSI DATABASE (CUKUP SATU BARIS)
DATABASE_URL="mysql+mysqlconnector://root:@localhost:3306/NAMA_DB"

# PENGATURAN APLIKASI
BASE_URL="http://127.0.0.1:8000"
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# PENGATURAN OTENTIKASI (JWT)
SECRET_KEY=""
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30